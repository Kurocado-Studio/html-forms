name: Code Quality

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  ci-lint:
    uses: 'kurocado-studio/styleguide/.github/workflows/lint.yml@latest'
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
